import Dexie from "dexie";
import { ExchangeRateI } from "./interface.js";
export default class ExchangeRateProvider extends Dexie {
    private fx;
    constructor();
    init(): Promise<this>;
    close(): Promise<this>;
    getInfo(): string;
    getRate(symbol: string): Promise<ExchangeRateI | undefined>;
    setRate(symbol: string, rate: number, ttl: number): Promise<boolean>;
}
//# sourceMappingURL=ExchangeRateProvider.d.ts.map