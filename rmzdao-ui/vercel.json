{
  "version": 2,
  "builds": [
    {
      "src": "rmzdao-ui/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "rmzdao-ui/build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
